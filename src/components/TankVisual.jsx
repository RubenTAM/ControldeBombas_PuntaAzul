// Tanque principal — dejado en blanco a propósito por ahora (pendiente de
// definir la visualización final). Se conserva la firma de props para no
// tener que tocar SystemDiagram cuando se vuelva a habilitar.
// eslint-disable-next-line no-unused-vars
export default function TankVisual({ level, thresholds, limits, volume, capacity }) {
  return <div className="h-[280px] w-[220px] rounded-2xl border border-dashed border-ink-200 bg-navy-50/40" />
}
